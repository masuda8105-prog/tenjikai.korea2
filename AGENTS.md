# AGENTS.md

## 目的と優先順位

このリポジトリは、GitHub Pages と Supabase で動く韓国展示会向け受注管理システムです。最優先は、会場で説明なしでも迷わず操作でき、通信不調や誤操作でも注文を失わないことです。変更前に `README.md`、`tests/README.md`、該当コード、既存マイグレーションを確認してください。実装と文書が食い違う場合は推測で本番仕様を変えず、差異を報告して互換的に直します。

## 構成

- `index.html`: お客様画面。CSS/JavaScriptを含む静的・モバイル優先UI。
- `staff.html` / `staff.js`: スタッフ画面、Supabase Auth、REST、Realtime（失敗時は10秒ポーリング）、印刷。
- `online-config.js`: ブラウザ公開可能な本番URL・Publishable key等の唯一の設定元。
- `product_master_korea.csv` / `product-images/`: 商品マスターと画像。CSVはUTF-8 BOMを許容する。
- `supabase/functions/`: 注文受付・期限切れ削除を行うサーバー側Edge Functions。
- `supabase/migrations/`: 本番DB変更の履歴。`supabase/sql/`は参照・運用用SQL。
- `tests/`: 静的検証とSupabaseをモックしたPlaywright E2E。

ビルド工程はありません。GitHub Pagesがルートの静的ファイルを直接配信します。外部CDN依存を増やさず、既存の `vendor/` を優先してください。

## 絶対に守る互換性・安全性

- 実テーブル名は常に `public.exhibition_orders`。`orders` 等へ改名しない。
- `order_data` は既存注文との契約である。未知のキーを保ったままマージし、欠落・`null`・旧形式を許容する。特に `orderNo`、顧客情報、`items` の短縮キー（`c`, `n`, `q`, `p`, `img`）、`total`、既存の履歴メタデータを壊さない。
- `order_no` 列と `order_data.orderNo` の既存値を維持する。受付番号はSupabase側で衝突なく採番し、ブラウザだけで連番を作らない。
- DB変更は既存列・テーブルを削除せず、追加マイグレーションを新規作成する。古いマイグレーションの書換えで本番状態を装わない。制約、列権限、RLS、Realtime、フロントのselect/PATCHを一緒に確認する。
- `online-config.js` を利用し、フロントエンドやGit履歴へ Secret key / `service_role` keyを絶対に置かない。`service_role` はEdge Functionの環境変数内だけで使う。お客様の登録はEdge Function経由、スタッフ操作はAuth＋`exhibition_staff`＋RLSで保護する。
- 通常の削除はRESTの `PATCH` によるソフトデリート（`status: "deleted"` と削除日時・担当者・削除理由・元statusの記録）。スタッフUIから `DELETE` しない。復元可能にする。物理削除は管理者専用または期限切れを処理するサーバー側cleanupに限定する。
- 状態を混同しない。`completed` は確認済み・代理店送信待ち、`sent` は送付済み。送付済みを編集したら履歴・元batch情報を残して `resend_required` にする。既存の `new` / `in_progress` も読めること。新statusを使う前にDBのCHECK制約とRLSを先に拡張する。
- 送信バッチは二重登録させず、可能ならDBトランザクション／一意制約／RPCで採番・確定し、送信時点の注文スナップショットを残す。更新競合は `updated_at` を条件にした楽観的ロックで検出する。
- 日本語・韓国語を含むテキストファイルはUTF-8で保存し、文字化けを残さない。商品CSVを不要に再保存・全件整形・改行変換しない。

## 会場UIの実装原則

- お客様画面は韓国語初期表示＋日本語切替、スマートフォン優先。主要操作は大きなタップ領域、明確な日本語／韓国語、3ステップ程度で完了させる。スタッフ画面もスマホ・タブレット・PCで使えること。
- 送信・保存・PDF・送付済み操作は開始直後にボタンを無効化し、`finally` で安全に戻す。連打、二重注文、同じ注文の複数バッチ登録を防ぐ。
- 通信中、成功、失敗、認証期限切れ、競合を明示する。失敗時はカート・入力・名刺選択・編集内容を消さず、再試行手段を示す。名刺付き注文はDBと画像の保存が完了するまで受付番号／成功画面を出さない。
- Realtime切断時もポーリングへフォールバックする。エラーを握りつぶして成功表示しない。
- 削除、送付済み、再送、復元など重大操作には対象・結果・取り消し可否が分かる確認を置く。A4 PDFは白背景・明瞭な罫線・注文ごとの改ページ・日本語文字化けなしで印刷確認する。
- アクセシビリティを保つ。ラベル、フォーカス、キーボード操作、`aria-live`、十分なコントラストを維持し、色だけで状態を伝えない。

## 検証

変更範囲に応じ、最低限次を実行します。

```powershell
python tests/static_validate.py
python tests/automated_e2e.py
node --check staff.js
node --check online-config.js
```

`automated_e2e.py` はPython Playwrightがある環境で実行します。`static_validate.py` は商品3,831件、KRW/100ウォン丸め、品番重複、画像2,753件、必須ファイル、`index.html` 内インラインJSを含む構文を検証します。商品仕様を意図的に変える場合だけ、根拠を示して期待値も更新します。

UI変更では390px、820px、PC幅を確認し、通常送信、必須項目、名刺あり／なし／失敗、二重クリック、ログイン切れ、一覧・検索・編集、status遷移、ソフト削除・復元、Realtime不通、A4印刷を手動確認します。DB/RLS変更後は匿名ユーザーが一覧・更新できず、許可スタッフだけが必要操作を行えることを実環境でスモークテストします。

## マイグレーション・公開

- 破壊的SQL、RLS緩和、秘密情報、既存データへの影響を差分レビューしてから適用する。変更順は原則「後方互換DB/Edge Function → フロント」。ロールバックまたは復元方法を用意する。
- `01_Supabase更新.ps1` は本番SupabaseへマイグレーションとFunctionsを反映するため、明示的な依頼・対象Project Ref確認・テスト完了なしに実行しない。
- `02_GitHub公開.ps1` は作業ツリー全体をstage/commit/pushするため、他人の変更を巻き込む。明示的な公開依頼なしに実行しない。公開前後は `明日_本番反映手順.md` の本番スモークテストを行う。
- 本番データの物理削除、履歴消去、キー変更、スタッフ権限追加は個別に承認を取る。

## エージェント間の協業

- 最初にファイル構成、注文登録、Auth/REST、DB列・RLS、`order_data`、status、名刺、印刷、削除、モバイル、既知不具合を確認し、影響範囲を共有してから編集する。
- サブエージェントは、明確に分離できる読取調査・テスト・レビューに限定して使う。同じファイルを複数エージェントで同時編集しない。担当ファイルと完了条件を明示する。
- 共有作業ツリーにある既存変更はユーザーまたは別担当のものとして保持する。勝手にrevert、reset、削除、全体整形をしない。
- 引継ぎ時は変更ファイル、DB影響、前提、実行した検証と結果、未実施の本番確認を簡潔に報告する。テスト未実施や失敗を「完了」と表現しない。
