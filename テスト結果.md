# テスト結果

実施日：2026-08-04

## 結果

### 自動ブラウザE2E：合格

Chromiumを使用し、Supabase通信をテスト用モックへ置き換えて確認しました。

- スマートフォン幅：商品検索 → カート追加 → お客様情報入力 → 注文送信
- サーバー発行の受付番号 `K260804-001` の表示
- 受付番号発行時は `submitted`、スタッフ確定時は `confirmed` になること
- 受付番号画面、注文控え、PNG、印刷用PDFへの受付番号・スタッフ確認用QRの反映
- 受付番号画面、注文控え、画像保存画面の3か所から注文編集へ戻れること
- 商品・数量・会社名・氏名・電話番号・発送先住所・備考・名刺画像が復元されること
- 修正時はPOSTを増やさずPATCHで同じ注文IDを更新し、受付番号とQRが変わらないこと
- スタッフ確定後はお客様側の修正が拒否されること
- 名刺付き注文の送信失敗時に、次の文言を表示して受付番号を発行しないこと

```text
名刺画像を送信できませんでした。再試行してください。
```

- スタッフログイン
- 確認待ち一覧の表示
- 注文詳細、会社名、品番、合計の表示
- `submitted → in_progress → confirmed` の状態更新
- QRから該当注文を直接開くこと
- 1件の印刷が1ページに収まり、QRと受付番号がフッター直前へ小さく表示されること
- スマホ印刷のヘッダー／フッター領域を含めても、一括送付PDFの表紙9項目が1ページに収まり、注文書が次ページから始まること
- 34明細の注文を複数ページで印刷し、空白ページがないこと
- 明細行、受付番号・QR、名刺、備考、合計欄へ改ページ抑止が設定されていること
- iPhone Chrome（390px）、Android Chrome（412px）、Windows Edge（1365px）、Mac Safari（1440px）のユーザーエージェント・画面幅で印刷用PDFを生成できること
- iPhone / Androidを分けず、すべてのスマートフォンで「画像を長押しして写真・画像を保存」の共通案内が最初に表示されること
- タブレット幅のお客様画面・スタッフ画面
- PC幅のスタッフ画面
- JavaScript実行時エラーがないこと

実行結果：

```text
E2E_PASS
```

### 静的検証：合格

- 商品マスター：3,831件
- CSV列数：13列
- 品番の空欄：0件
- 品番重複：0件
- 通貨：全件KRW
- 価格：全件100ウォン単位
- 商品画像：2,753ファイル
- CSVに記載された画像の参照切れ：0件
- `index.html` 内JavaScript構文
- `staff.js` 構文
- `online-config.js` 構文
- Edge Functions 2本のTypeScript構文
- 必須ファイルと必須機能の存在
- Secret key / service_role keyが公開ファイルへ混入していないこと

実行結果：

```text
STATIC_VALIDATION_PASS
```

## 実環境で残る最終確認

新しいDBマイグレーションとEdge Functionsは、まだ本番Supabaseへ反映されていないため、**本番Supabaseを使用した実通信テストだけはGitHub公開前後に1回必要**です。自動テストのiPhone / Android / Mac確認はChromium上の端末エミュレーションであり、各OS固有の印刷・共有ダイアログは実機で確認します。

`明日_本番反映手順.md`の順番で反映後、次を確認してください。

1. お客様画面から名刺付きテスト注文を送信
2. 受付番号が表示される
3. スタッフ画面の確認待ちへ表示される
4. 原寸名刺を開ける
5. 対応開始・注文確定・印刷が動く
6. 確定前の修正が同じ注文IDへ反映され、確定後は拒否される
7. QRからスタッフ画面の該当注文が直接開く
8. iPhone、Android、Windows PC、Macの実機で印刷プレビューと保存・共有を確認する

これは未完成機能ではなく、本番環境へ新コードをデプロイした後にしか実施できない最終スモークテストです。
