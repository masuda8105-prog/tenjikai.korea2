# テスト結果

実施日：2026-07-30

## 結果

### 自動ブラウザE2E：合格

Chromiumを使用し、Supabase通信をテスト用モックへ置き換えて確認しました。

- スマートフォン幅：商品検索 → カート追加 → お客様情報入力 → 注文送信
- サーバー発行の受付番号 `K260730-001` の表示
- 注文控えへの受付番号・会社名・品番の反映
- 名刺付き注文の送信失敗時に、次の文言を表示して受付番号を発行しないこと

```text
名刺画像を送信できませんでした。再試行してください。
```

- スタッフログイン
- NEW一覧の表示
- 注文詳細、会社名、品番、合計の表示
- `NEW → 対応中 → 完了` の状態更新
- 印刷処理
- タブレット幅のお客様画面・スタッフ画面
- PC幅のスタッフ画面
- JavaScript実行時エラーがないこと

実行結果：

```text
E2E_PASS
```

### 静的検証：合格

- 商品マスター：3,831件
- CSV列数：13列
- 品番の空欄：0件
- 品番重複：0件
- 通貨：全件KRW
- 価格：全件100ウォン単位
- 商品画像：2,753ファイル
- CSVに記載された画像の参照切れ：0件
- `index.html` 内JavaScript構文
- `staff.js` 構文
- `online-config.js` 構文
- Edge Functions 2本のTypeScript構文
- 必須ファイルと必須機能の存在
- Secret key / service_role keyが公開ファイルへ混入していないこと

実行結果：

```text
STATIC_VALIDATION_PASS
```

## 実環境で残る最終確認

新しいDBマイグレーションとEdge Functionsは、まだ本番Supabaseへ反映されていないため、**本番Supabaseを使用した実通信テストだけはGitHub公開前後に1回必要**です。

`明日_本番反映手順.md`の順番で反映後、次を確認してください。

1. お客様画面から名刺付きテスト注文を送信
2. 受付番号が表示される
3. スタッフ画面のNEWへ表示される
4. 原寸名刺を開ける
5. 対応開始・完了・印刷が動く

これは未完成機能ではなく、本番環境へ新コードをデプロイした後にしか実施できない最終スモークテストです。
