# 明日、会社で行う本番反映

> v3.0更新：最初に `README_Supabase設定.md` を読み、**DB → Edge Functions → cleanup secret/Cron → GitHub Pages** の順で反映してください。

作業対象は、既存GitHubリポジトリです。

```text
korea-exibition-sannishimura
```

ZIPを解凍し、**ZIP内のファイルとフォルダーを既存リポジトリのルートへ上書き**してください。

---

## 1. Gitが入っていない場合

PowerShellを管理者で開きます。

```powershell
winget install --id Git.Git -e --source winget
```

インストール後、PowerShellを閉じて開き直します。

```powershell
git --version
```

バージョンが表示されれば完了です。

---

## 2. Supabaseを更新する

既存リポジトリのフォルダーでPowerShellを開き、次を実行します。

```powershell
.\01_Supabase更新.ps1
```

このスクリプトが行うこと：

- Supabase login / link
- Edge Function用設定値の反映
- DBマイグレーション
- 受付番号の自動採番
- スタッフ用RLS
- Realtime設定
- 14日後の自動削除Cron
- `exhibition-order` 再デプロイ
- `cleanup-orders` 再デプロイ

追加の `20260803120000_exhibition_order_workflow.sql` により、ソフト削除・復元、送付済み・再送待ち、修正履歴、送信バッチ、冪等IDが追加されます。

実行後、`README_Supabase設定.md` の「cleanup secretとCron」を必ず行ってください。公開用Publishable keyで起動する旧Cronは新マイグレーションで停止します。

途中でブラウザログインを求められた場合は、Supabaseへログインしてください。

---

## 3. スタッフアカウントを作る

Supabase Dashboardで次を開きます。

```text
Authentication → Users → Add user
```

スタッフのメールアドレスとパスワードを登録します。

続いてSQL Editorで、メールアドレスと表示名を変更して実行します。

```sql
insert into public.exhibition_staff (user_id, display_name)
select id, '増田'
from auth.users
where email = 'スタッフのメールアドレス'
on conflict (user_id)
do update set display_name = excluded.display_name, active = true;
```

登録したユーザーだけが注文一覧を閲覧できます。

---

## 4. GitHubへ公開する

```powershell
.\02_GitHub公開.ps1
```

現在のブランチを自動判定し、`git add → commit → push`を実行します。

---

## 5. 公開後の最終確認

お客様画面：

```text
https://masuda8105-prog.github.io/korea-exibition-sannishimura/
```

スタッフ画面：

```text
https://masuda8105-prog.github.io/korea-exibition-sannishimura/staff.html
```

GitHub Pagesの反映には数分かかる場合があります。

### 本番スモークテスト

1. スタッフ画面へログイン
2. お客様画面で商品を1点追加
3. テスト用の会社名・氏名・電話番号を入力
4. 名刺を1枚撮影
5. 注文を送信
6. `K日付-連番`の受付番号が表示される
7. スタッフ画面の「確認待ち」へ表示される
8. 名刺プレビューをタップして原寸画像を確認
9. `対応開始 → 注文を確定する`を操作
10. 「削除履歴へ移す」→ 削除履歴から復元を確認
11. 日付を選び、一括送付ダイアログのPDF印刷・メール本文・送付済み登録を確認
12. 送付済み注文を修正し、「修正版・再送待ち」になることを確認

テスト注文は14日後の削除対象です。

---

## 困った場合

- `git`が見つからない：手順1を実行後、PowerShellを開き直す
- スタッフ画面に注文が出ない：スタッフを`exhibition_staff`へ登録したか確認
- ログインできない：Authenticationのメールアドレス・パスワードを確認
- 送信できない：Edge Functionsのログと`ALLOWED_ORIGINS`を確認
